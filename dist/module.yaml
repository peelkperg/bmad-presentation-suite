code: cis
dependencies:
  - cis
name: "Presentation Suite: Visual Communication & Storytelling"
header: "Professional presentations powered by Caravaggio — message-first, visually purposeful"
subheader: "Extension of CIS — requires CIS module installed as a peer dependency"
default_selected: false

# Variables from Core Config inserted:
## user_name
## communication_language
## document_output_language
## output_folder

enable_presentation_hook:
  prompt: "Enable the post-workflow presentation hook? After any BMAD workflow produces a document, Caravaggio will offer to turn it into a presentation."
  default: true
  result: "{value}"
